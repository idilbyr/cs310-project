name: cs310_project
description: Fridge App Project Step 2
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # BURASI ÇOK ÖNEMLİ:
  # Senin yüklediğin logo.png'yi görmesi için bu satır şart.
  assets:
    - assets/

  # Roboto fontunu şimdilik kapalı tutuyorum ki dosya yoksa hata vermesin.
  # Eğer font dosyasını da yüklersen başındaki # işaretlerini kaldırabilirsin.
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: fonts/Roboto-Regular.ttf
